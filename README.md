# Study Bloom V8
Clean offline-first PWA rebuild. Run with `python3 -m http.server 8080`, visit `http://localhost:8080`, then install with the browser's Add to Home Screen or Install command. Deploy the folder to any HTTPS static host for phone installation. All planner data is stored locally. Use Settings to export backups.
